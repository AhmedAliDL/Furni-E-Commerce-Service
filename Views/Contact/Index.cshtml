@model ContactViewModel
@inject UserManager<User> _userManager
@{
    ViewData["Title"] = "Index";
    var email = User.Identity.Name;
    var currentUser = await _userManager.FindByEmailAsync(email!);
}

<!-- End Header/Navigation -->
<!-- Start Hero Section -->
<div class="hero">
    <div class="container">
        <div class="row justify-content-between">
            <div class="col-lg-5">
                <div class="intro-excerpt">
                    <h1>Contact</h1>
                    <p class="mb-4">Donec vitae odio quis nisl dapibus malesuada. Nullam ac aliquet velit. Aliquam vulputate velit imperdiet dolor tempor tristique.</p>
                    <p><a href="" class="btn btn-secondary me-2">Shop Now</a><a href="#" class="btn btn-white-outline">Explore</a></p>
                </div>
            </div>
            <div class="col-lg-7">
                <div class="hero-img-wrap">
                    <img src="/images/couch.png" class="img-fluid">
                </div>
            </div>
        </div>
    </div>
</div>
<!-- End Hero Section -->
<!-- Start Contact Form -->
<div class="untree_co-section">
    <div class="container">

        <div class="block">
            <div class="row justify-content-center">


                <div class="col-md-8 col-lg-8 pb-4">


                    <div class="row mb-5">
                        <div class="col-lg-4">
                            <div class="service no-shadow align-items-center link horizontal d-flex active" data-aos="fade-left" data-aos-delay="0">
                                <div class="service-icon color-1 mb-4">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-geo-alt-fill" viewBox="0 0 16 16">
                                        <path d="M8 16s6-5.686 6-10A6 6 0 0 0 2 6c0 4.314 6 10 6 10zm0-7a3 3 0 1 1 0-6 3 3 0 0 1 0 6z" />
                                    </svg>
                                </div> <!-- /.icon -->
                                <div class="service-contents">
                                    <p>12 Al Nasr Street, Maadi, Cairo, Egypt</p>
                                </div> <!-- /.service-contents-->
                            </div> <!-- /.service -->
                        </div>

                        <div class="col-lg-4">
                            <div class="service no-shadow align-items-center link horizontal d-flex active" data-aos="fade-left" data-aos-delay="0">
                                <div class="service-icon color-1 mb-4">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-envelope-fill" viewBox="0 0 16 16">
                                        <path d="M.05 3.555A2 2 0 0 1 2 2h12a2 2 0 0 1 1.95 1.555L8 8.414.05 3.555zM0 4.697v7.104l5.803-3.558L0 4.697zM6.761 8.83l-6.57 4.027A2 2 0 0 0 2 14h12a2 2 0 0 0 1.808-1.144l-6.57-4.027L8 9.586l-1.239-.757zm3.436-.586L16 11.801V4.697l-5.803 3.546z" />
                                    </svg>
                                </div> <!-- /.icon -->
                                <div class="service-contents">
                                    <p>ahmedalibahaa2003
                                        @@gmail.com</p>
                                    
                                </div> <!-- /.service-contents-->
                            </div> <!-- /.service -->
                        </div>
                        
                        <div class="col-lg-4">
                            <div class="service no-shadow align-items-center link horizontal d-flex active" data-aos="fade-left" data-aos-delay="0">
                                <div class="service-icon color-1 mb-4">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-telephone-fill" viewBox="0 0 16 16">
                                        <path fill-rule="evenodd" d="M1.885.511a1.745 1.745 0 0 1 2.61.163L6.29 2.98c.329.423.445.974.315 1.494l-.547 2.19a.678.678 0 0 0 .178.643l2.457 2.457a.678.678 0 0 0 .644.178l2.189-.547a1.745 1.745 0 0 1 1.494.315l2.306 1.794c.829.645.905 1.87.163 2.611l-1.034 1.034c-.74.74-1.846 1.065-2.877.702a18.634 18.634 0 0 1-7.01-4.42 18.634 18.634 0 0 1-4.42-7.009c-.362-1.03-.037-2.137.703-2.877L1.885.511z" />
                                    </svg>
                                </div> <!-- /.icon -->
                                <div class="service-contents">
                                    <p>+20 01096458798</p>
                                </div> <!-- /.service-contents-->
                            </div> <!-- /.service -->
                        </div>
                    </div>

                    <form id="contactForm" method="post" asp-action="SendEmail" asp-controller="Contact">
                        <div class="row">
                            <div class="col-6">
                                <div class="form-group">
                                    <label class="text-black" for="fname">First name</label>
                                    <input disabled value="@currentUser.FName" type="text" class="form-control" id="fname">
                                </div>
                            </div>
                            <div class="col-6">
                                <div class="form-group">
                                    <label class="text-black" for="lname">Last name</label>
                                    <input disabled value="@currentUser.LName" type="text" class="form-control" id="lname">
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="text-black" for="email">Email address</label>
                            <input disabled value="@currentUser.Email" type="email" class="form-control" id="email">
                        </div>
                        <div class="form-group">
                            <label class="text-black" for="subject">Subject</label>
                            <input asp-for="Subject" type="text" class="form-control" id="subject">
                            <span asp-validation-for="Subject" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-5">
                            <label class="text-black" for="message">Message</label>
                            <textarea asp-for="Body" class="form-control" id="message" cols="30" rows="5"></textarea>
                            <span asp-validation-for="Body" class="text-danger"></span>
                        </div>

                        <div class="text-center">
                            <input type="submit" id="submitBtn" class="btn btn-primary-hover-outline" value="Send Message" />
                            <input type="hidden" id="userId" value="@currentUser.Id" />
                        </div>

                    </form>
                    <div id="cooldownMessage" class="alert alert-warning" style="display:none;">
                        You have submitted your message. Please wait 1 minute before sending another.
                    </div>

                </div>

            </div>

        </div>

    </div>


</div>

<!-- End Contact Form -->

<script src="/js/bootstrap.bundle.min.js"></script>
<script src="/js/tiny-slider.js"></script>
<script src="/js/custom.js"></script>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const submitBtn = document.getElementById("submitBtn");
        const userId = document.getElementById("userId").value;
        const cooldownMessage = document.getElementById("cooldownMessage");
        const contactForm = document.getElementById("contactForm");
        const subjectField = document.getElementById("subject");
        const bodyField = document.getElementById("message");

        // Check for existing cooldown in localStorage
        const cooldownEndTime = localStorage.getItem(`cooldownEndTime_${userId}`);
        if (cooldownEndTime && new Date().getTime() < cooldownEndTime) {
            const remainingTime = cooldownEndTime - new Date().getTime();
            disableButton(remainingTime);
        }

        // Function to disable the button and show cooldown message
        function disableButton(milliseconds) {
            submitBtn.disabled = true;
            submitBtn.textContent = "Please wait before sending another message...";
            cooldownMessage.style.display = "block";

            setTimeout(() => {
                submitBtn.disabled = false;
                submitBtn.textContent = "Send Message";
                cooldownMessage.style.display = "none";
                localStorage.removeItem(`cooldownEndTime_${userId}`);
            }, milliseconds);
        }

        // Form submission handler
        contactForm.onsubmit = async function (event) {
            event.preventDefault(); // Prevent the default form submission

            // Immediately show the cooldown message and disable the button
            const cooldownTime = 1 * 60 * 1000; // 1 minute in milliseconds
            disableButton(cooldownTime);
            localStorage.setItem(`cooldownEndTime_${userId}`, new Date().getTime() + cooldownTime);

            // Proceed with the form submission (using fetch for async call)
            const formData = new FormData(this);
            const response = await fetch(this.action, {
                method: 'POST',
                body: formData
            });

            const result = await response.json();

            if (!result.success) {
                const remainingTime = result.remainingTime;
                localStorage.setItem(`cooldownEndTime_${userId}`, new Date().getTime() + remainingTime);
                disableButton(remainingTime);
            } else {
                // Handle successful form submission
                submitBtn.textContent = "Message Sent!";

                // Clear the subject and body fields
                subjectField.value = "";
                bodyField.value = "";
            }
        };
    });
</script>
@section Scripts
{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}